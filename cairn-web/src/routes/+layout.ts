// Pure client-side SPA: no server-side rendering. The session views, stores,
// and terminal arrive in later tasks; this only establishes the static shell.
export const ssr = false;
export const prerender = false;
